[
  {"question": "List all artists.", "ground_truth_sql": "SELECT * FROM Artist"},
  {"question": "List all albums.", "ground_truth_sql": "SELECT * FROM Album"},
  {"question": "How many artists are there?", "ground_truth_sql": "SELECT COUNT(*) AS total_artists FROM Artist"},
  {"question": "How many albums are there?", "ground_truth_sql": "SELECT COUNT(*) AS total_albums FROM Album"},
  {"question": "How many tracks are there?", "ground_truth_sql": "SELECT COUNT(*) AS total_tracks FROM Track"},
  {"question": "Show all genres.", "ground_truth_sql": "SELECT * FROM Genre"},
  {"question": "Show all media types.", "ground_truth_sql": "SELECT * FROM MediaType"},
  {"question": "List all playlists.", "ground_truth_sql": "SELECT * FROM Playlist"},
  {"question": "How many customers are there?", "ground_truth_sql": "SELECT COUNT(*) AS total_customers FROM Customer"},
  {"question": "How many employees are there?", "ground_truth_sql": "SELECT COUNT(*) AS total_employees FROM Employee"},
  {"question": "List all customers from Brazil.", "ground_truth_sql": "SELECT * FROM Customer WHERE Country = 'Brazil'"},
  {"question": "List all customers from Germany.", "ground_truth_sql": "SELECT * FROM Customer WHERE Country = 'Germany'"},
  {"question": "Show the first name and last name of all employees.", "ground_truth_sql": "SELECT FirstName, LastName FROM Employee"},
  {"question": "Find all tracks in the Rock genre.", "ground_truth_sql": "SELECT Track.Name FROM Track JOIN Genre ON Track.GenreId = Genre.GenreId WHERE Genre.Name = 'Rock'"},
  {"question": "Find all tracks in the Jazz genre.", "ground_truth_sql": "SELECT Track.Name FROM Track JOIN Genre ON Track.GenreId = Genre.GenreId WHERE Genre.Name = 'Jazz'"},
  {"question": "List all albums by the artist AC/DC.", "ground_truth_sql": "SELECT Album.Title FROM Album JOIN Artist ON Album.ArtistId = Artist.ArtistId WHERE Artist.Name = 'AC/DC'"},
  {"question": "List all albums by the artist Metallica.", "ground_truth_sql": "SELECT Album.Title FROM Album JOIN Artist ON Album.ArtistId = Artist.ArtistId WHERE Artist.Name = 'Metallica'"},
  {"question": "Show the total number of tracks per genre.", "ground_truth_sql": "SELECT Genre.Name, COUNT(Track.TrackId) AS total_tracks FROM Track JOIN Genre ON Track.GenreId = Genre.GenreId GROUP BY Genre.Name"},
  {"question": "Show the total number of albums per artist.", "ground_truth_sql": "SELECT Artist.Name, COUNT(Album.AlbumId) AS total_albums FROM Album JOIN Artist ON Album.ArtistId = Artist.ArtistId GROUP BY Artist.Name"},
  {"question": "What is the most expensive track?", "ground_truth_sql": "SELECT Name, UnitPrice FROM Track ORDER BY UnitPrice DESC LIMIT 1"},
  {"question": "What are the top 5 longest tracks?", "ground_truth_sql": "SELECT Name, Milliseconds FROM Track ORDER BY Milliseconds DESC LIMIT 5"},
  {"question": "List all invoices from 2010.", "ground_truth_sql": "SELECT * FROM Invoice WHERE strftime('%Y', InvoiceDate) = '2010'"},
  {"question": "What is the total revenue from all invoices?", "ground_truth_sql": "SELECT SUM(Total) AS total_revenue FROM Invoice"},
  {"question": "Which country has the most customers?", "ground_truth_sql": "SELECT Country, COUNT(*) AS total FROM Customer GROUP BY Country ORDER BY total DESC LIMIT 1"},
  {"question": "Show all tracks in the playlist Music.", "ground_truth_sql": "SELECT Track.Name FROM Track JOIN PlaylistTrack ON Track.TrackId = PlaylistTrack.TrackId JOIN Playlist ON PlaylistTrack.PlaylistId = Playlist.PlaylistId WHERE Playlist.Name = 'Music'"},
  {"question": "Show all tracks in the playlist Movies.", "ground_truth_sql": "SELECT Track.Name FROM Track JOIN PlaylistTrack ON Track.TrackId = PlaylistTrack.TrackId JOIN Playlist ON PlaylistTrack.PlaylistId = Playlist.PlaylistId WHERE Playlist.Name = 'Movies'"},
  {"question": "How many tracks are in each playlist?", "ground_truth_sql": "SELECT Playlist.Name, COUNT(PlaylistTrack.TrackId) AS track_count FROM Playlist JOIN PlaylistTrack ON Playlist.PlaylistId = PlaylistTrack.PlaylistId GROUP BY Playlist.Name"},
  {"question": "List the names of all customers who have made an invoice.", "ground_truth_sql": "SELECT DISTINCT Customer.FirstName, Customer.LastName FROM Customer JOIN Invoice ON Customer.CustomerId = Invoice.CustomerId"},
  {"question": "What is the average invoice total?", "ground_truth_sql": "SELECT AVG(Total) AS avg_total FROM Invoice"},
  {"question": "Which employee has the most customers assigned?", "ground_truth_sql": "SELECT Employee.FirstName, Employee.LastName, COUNT(Customer.CustomerId) AS total FROM Employee JOIN Customer ON Employee.EmployeeId = Customer.SupportRepId GROUP BY Employee.EmployeeId ORDER BY total DESC LIMIT 1"},
  {"question": "List all tracks composed by Angus Young.", "ground_truth_sql": "SELECT Name FROM Track WHERE Composer = 'Angus Young'"},
  {"question": "Show the total sales per country.", "ground_truth_sql": "SELECT BillingCountry, SUM(Total) AS total_sales FROM Invoice GROUP BY BillingCountry ORDER BY total_sales DESC"},
  {"question": "How many invoices were made in each year?", "ground_truth_sql": "SELECT strftime('%Y', InvoiceDate) AS year, COUNT(*) AS total FROM Invoice GROUP BY year"},
  {"question": "Find customers who spent more than 40 dollars in total.", "ground_truth_sql": "SELECT Customer.FirstName, Customer.LastName, SUM(Invoice.Total) AS total_spent FROM Customer JOIN Invoice ON Customer.CustomerId = Invoice.CustomerId GROUP BY Customer.CustomerId HAVING total_spent > 40"},
  {"question": "List tracks that cost more than 1 dollar.", "ground_truth_sql": "SELECT Name, UnitPrice FROM Track WHERE UnitPrice > 1"},
  {"question": "Show albums that have more than 10 tracks.", "ground_truth_sql": "SELECT Album.Title, COUNT(Track.TrackId) AS track_count FROM Album JOIN Track ON Album.AlbumId = Track.AlbumId GROUP BY Album.AlbumId HAVING track_count > 10"},
  {"question": "List all artists who have more than 5 albums.", "ground_truth_sql": "SELECT Artist.Name, COUNT(Album.AlbumId) AS album_count FROM Artist JOIN Album ON Artist.ArtistId = Album.ArtistId GROUP BY Artist.ArtistId HAVING album_count > 5"},
  {"question": "What are the unique billing countries in invoices?", "ground_truth_sql": "SELECT DISTINCT BillingCountry FROM Invoice ORDER BY BillingCountry"},
  {"question": "Show the top 10 customers by total spending.", "ground_truth_sql": "SELECT Customer.FirstName, Customer.LastName, SUM(Invoice.Total) AS total_spent FROM Customer JOIN Invoice ON Customer.CustomerId = Invoice.CustomerId GROUP BY Customer.CustomerId ORDER BY total_spent DESC LIMIT 10"},
  {"question": "Find all tracks that have never been sold.", "ground_truth_sql": "SELECT Track.Name FROM Track WHERE Track.TrackId NOT IN (SELECT TrackId FROM InvoiceLine)"},
  {"question": "What is the most popular genre by number of tracks sold?", "ground_truth_sql": "SELECT Genre.Name, COUNT(InvoiceLine.TrackId) AS total_sold FROM InvoiceLine JOIN Track ON InvoiceLine.TrackId = Track.TrackId JOIN Genre ON Track.GenreId = Genre.GenreId GROUP BY Genre.Name ORDER BY total_sold DESC LIMIT 1"},
  {"question": "Show the total revenue per genre.", "ground_truth_sql": "SELECT Genre.Name, SUM(InvoiceLine.UnitPrice * InvoiceLine.Quantity) AS revenue FROM InvoiceLine JOIN Track ON InvoiceLine.TrackId = Track.TrackId JOIN Genre ON Track.GenreId = Genre.GenreId GROUP BY Genre.Name ORDER BY revenue DESC"},
  {"question": "List all tracks from the album Nevermind.", "ground_truth_sql": "SELECT Track.Name FROM Track JOIN Album ON Track.AlbumId = Album.AlbumId WHERE Album.Title = 'Nevermind'"},
  {"question": "Which artist has the most tracks?", "ground_truth_sql": "SELECT Artist.Name, COUNT(Track.TrackId) AS total_tracks FROM Artist JOIN Album ON Artist.ArtistId = Album.ArtistId JOIN Track ON Album.AlbumId = Track.AlbumId GROUP BY Artist.ArtistId ORDER BY total_tracks DESC LIMIT 1"},
  {"question": "Show all customers in alphabetical order by last name.", "ground_truth_sql": "SELECT FirstName, LastName FROM Customer ORDER BY LastName ASC"},
  {"question": "List all employees hired after the year 2003.", "ground_truth_sql": "SELECT FirstName, LastName, HireDate FROM Employee WHERE strftime('%Y', HireDate) > '2003'"},
  {"question": "What is the average track length in minutes?", "ground_truth_sql": "SELECT AVG(Milliseconds) / 60000.0 AS avg_length_minutes FROM Track"},
  {"question": "Which album has the most tracks?", "ground_truth_sql": "SELECT Album.Title, COUNT(Track.TrackId) AS track_count FROM Album JOIN Track ON Album.AlbumId = Track.AlbumId GROUP BY Album.AlbumId ORDER BY track_count DESC LIMIT 1"},
  {"question": "Show all invoice line items for invoice ID 1.", "ground_truth_sql": "SELECT * FROM InvoiceLine WHERE InvoiceId = 1"},
  {"question": "List all tracks available in MP3 format.", "ground_truth_sql": "SELECT Track.Name FROM Track JOIN MediaType ON Track.MediaTypeId = MediaType.MediaTypeId WHERE MediaType.Name = 'MPEG audio file'"}
]
